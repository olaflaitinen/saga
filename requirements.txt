# Runtime dependencies for saga-forecast.
# This file is the pip-installable subset of environment.yaml.
# For a full reproducible environment (including CUDA-enabled PyTorch and conda packages),
# use: conda env create -f environment.yaml
# See docs/reproducibility/docker-environment.md for the full dependency rationale.

torch>=2.4
numpy>=1.26,<2.0
pandas>=2.2
pyarrow>=15
scipy>=1.13
scikit-learn>=1.5
lightgbm>=4.3
structlog>=24.1
pydantic>=2.6
pyyaml>=6.0
